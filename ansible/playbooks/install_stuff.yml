---
- hosts: all
  become: true
  tasks:

  - name: install tmux package
    ansible.builtin.apt:
      name: tmux
      state: latest
      update_cache: yes
    when: ansible_distribution == "Ubuntu"

  - name: install tmux package
    ansible.builtin.dnf:
      name: tmux
      state: latest
      update_cache: yes
    when: ansible_distribution == "Fedora"
